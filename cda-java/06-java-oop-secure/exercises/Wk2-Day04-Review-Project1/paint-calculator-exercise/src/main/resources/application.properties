spring.application.name=paint-calculator
spring.profiles.active=${PAINT_PROFILE:memory}
spring.main.banner-mode=off
spring.datasource.url=${PAINT_DB_URL:jdbc:postgresql://localhost:5432/paint_calculator}
spring.datasource.username=${PAINT_DB_USER:paint_app}
spring.datasource.password=${PAINT_DB_PASSWORD:change-me}
